import 'package:flutter/material.dart';

class MyTimelineTime extends StatelessWidget {
  const MyTimelineTime({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: ListView(children: [MyTimelineTime()]));
  }
}
